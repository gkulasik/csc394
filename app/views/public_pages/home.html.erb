


<ul class="orbit" data-orbit>
  <% @items.each do |i| %>
  <li>
    <center><%= link_to image_tag(i.images.first.img.url(:medium), alt: i.title), i %></center>
    <div class="orbit-caption">
      <%= i.title %>
    </div>
  </li>
 <%end%>
</ul>
